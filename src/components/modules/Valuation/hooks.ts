import { useState, useEffect, useCallback, useMemo } from 'react'

export function useValuation() {


  return {
    activeTab,
    appraisals,
    completed,
    createOpen,
    criteria,
    currentScore,
    d,
    deptAp,
    deptAverages,
    deptFilter,
    employees,
    filteredAppraisals,
    handleCreate,
    interactive,
    k,
    loadData,
    loading,
    mockPeriods,
    pending,
    periodFilter,
    ratingDistribution,
    scores,
    search,
    selected,
    setActiveTab,
    setCreateOpen,
    setDeptFilter,
    setPeriodFilter,
    setStatusFilter,
    setViewOpen,
    stCfg,
    star,
    statusFilter,
    topPerformers,
    val,
    viewOpen,
  }
}
