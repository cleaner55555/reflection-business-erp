import { useState, useEffect, useCallback, useMemo } from 'react'

export function useDashboard() {
  const {false, groupedActivity, i, kpiCards, lowStock, monthlyRevenueChart, pieData, quickActions, recentInvoices} = useDashboard()

  return {
    0,
    3,
    50,
    8,
    80,
    container,
    groupedActivity,
    i,
    item,
    kpiCards,
    lowStock,
    monthlyRevenueChart,
    pieData,
    quickActions,
    recentInvoices,
  }
}
