// ============================================================// TimeBilling Module – Mock Data & Business Logic Helpers// ============================================================import type {  Client, Project, Employee, TimeEntry, Invoice,  TimeBillingSettings, InvoiceItem,  RevenueByClient, RevenueByProject, RevenueByEmployee, MonthlySummary,  BillingStatus, InvoiceStatus, PaymentTerms,} from './types'// ---------- Default Settings ----------export const DEFAULT_SETTINGS: TimeBillingSettings = {  defaultHourlyRate: 5000,       // RSD/hour  pdvRate: 20,                    // 20% PDV  paymentTerms: 'net30',  currency: 'RSD',  invoicePrefix: 'Fakt',  nextInvoiceNumber: 1,  roundTo: '0.5',  workDayHours: 8,}// ---------- Mock Clients ----------export const mockClients: Client[] = [  {    id: 'cl-1', name: 'Delta Inženjering DOO', pib: '108001234', mb: '20045678',    address: 'Булевар Михајла Пупина 10', city: 'Београд', defaultRate: 5500, isActive: true,  },  {    id: 'cl-2', name: 'Nexe Telekomunikacije DOO', pib: '108005678', mb: '20078901',    address: 'Улица Краља Милана 24', city: 'Нови Сад', defaultRate: 6000, isActive: true,  },  {    id: 'cl-3', name: 'Vega Solutions DOO', pib: '108009012', mb: '20012345',    address: 'Студентски трг 5', city: 'Ниш', defaultRate: 4500, isActive: true,  },  {    id: 'cl-4', name: 'Alfa Banka AD', pib: '108001345', mb: '20123456',    address: 'Теразије 3', city: 'Београд', defaultRate: 7000, isActive: true,  },  {    id: 'cl-5', name: 'Horizont Invest DOO', pib: '108001678', mb: '20034567',    address: 'Булевар ослобођења 85', city: 'Суботица', defaultRate: 4800, isActive: false,  },]// ---------- Mock Projects ----------export const mockProjects: Project[] = [  {    id: 'pr-1', clientId: 'cl-1', name: 'ERP Мigracija', code: 'PRJ-2024-001',    description: 'Миграција старог ERP система на нову платформу', hourlyRate: 5500,    isBillable: true, isCompleted: false, startDate: '2024-01-15', endDate: null,  },  {    id: 'pr-2', clientId: 'cl-2', name: 'Мобилна апликација', code: 'PRJ-2024-002',    description: 'Развој мобилне апликације за клијенте', hourlyRate: 6000,    isBillable: true, isCompleted: false, startDate: '2024-02-01', endDate: '2024-06-30',  },  {    id: 'pr-3', clientId: 'cl-3', name: 'Web Портал', code: 'PRJ-2024-003',    description: 'Корпоративни web портал', hourlyRate: 4500,    isBillable: true, isCompleted: true, startDate: '2023-09-01', endDate: '2024-03-31',  },  {    id: 'pr-4', clientId: 'cl-4', name: 'Безбедносни аудит', code: 'PRJ-2024-004',    description: 'Безбедносни аудит и поправке', hourlyRate: 7000,    isBillable: true, isCompleted: false, startDate: '2024-03-10', endDate: null,  },  {    id: 'pr-5', clientId: 'cl-1', name: ' CRM Интеграција', code: 'PRJ-2024-005',    description: 'Интеграција CRM са ERP системом', hourlyRate: 5200,    isBillable: true, isCompleted: false, startDate: '2024-04-01', endDate: null,  },]// ---------- Mock Employees ----------export const mockEmployees: Employee[] = [  { id: 'emp-1', name: 'Марко Петровић', role: 'Сениор програмер', defaultRate: 5500, isActive: true },  { id: 'emp-2', name: 'Јована Николић', role: 'Програмер', defaultRate: 4500, isActive: true },  { id: 'emp-3', name: 'Александар Стојановић', role: 'Пројект менаџер', defaultRate: 6500, isActive: true },  { id: 'emp-4', name: 'Маја Јовановић', role: 'UI/UX Дизајнер', defaultRate: 5000, isActive: true },  { id: 'emp-5', name: 'Ненад Ристић', role: 'QA Инжењер', defaultRate: 4000, isActive: false },]// ---------- Mock Time Entries ----------export const mockTimeEntries: TimeEntry[] = [  {    id: 'te-1', employeeId: 'emp-1', clientId: 'cl-1', projectId: 'pr-1',    date: '2024-11-04', hours: 8, rate: 5500, description: 'Кодирање модула миграције – корисници',    billingStatus: 'unbilled', invoiceId: null, createdAt: '2024-11-04T17:30:00Z', updatedAt: '2024-11-04T17:30:00Z',  },  {    id: 'te-2', employeeId: 'emp-2', clientId: 'cl-1', projectId: 'pr-1',    date: '2024-11-04', hours: 6, rate: 5500, description: 'Тестирање модула миграције',    billingStatus: 'unbilled', invoiceId: null, createdAt: '2024-11-04T18:00:00Z', updatedAt: '2024-11-04T18:00:00Z',  },  {    id: 'te-3', employeeId: 'emp-1', clientId: 'cl-2', projectId: 'pr-2',    date: '2024-11-05', hours: 4, rate: 6000, description: 'API дизајн за мобилну апликацију',    billingStatus: 'unbilled', invoiceId: null, createdAt: '2024-11-05T16:00:00Z', updatedAt: '2024-11-05T16:00:00Z',  },  {    id: 'te-4', employeeId: 'emp-3', clientId: 'cl-4', projectId: 'pr-4',    date: '2024-11-05', hours: 7, rate: 7000, description: 'Безбедносни преглед инфраструктуре',    billingStatus: 'unbilled', invoiceId: null, createdAt: '2024-11-05T18:30:00Z', updatedAt: '2024-11-05T18:30:00Z',  },  {    id: 'te-5', employeeId: 'emp-4', clientId: 'cl-1', projectId: 'pr-5',    date: '2024-11-06', hours: 5, rate: 5200, description: 'Дизајн CRM корисничког интерфејса',    billingStatus: 'unbilled', invoiceId: null, createdAt: '2024-11-06T15:00:00Z', updatedAt: '2024-11-06T15:00:00Z',  },  {    id: 'te-6', employeeId: 'emp-1', clientId: 'cl-1', projectId: 'pr-1',    date: '2024-10-28', hours: 8, rate: 5500, description: 'Рефакторисање базе података',    billingStatus: 'billed', invoiceId: null, createdAt: '2024-10-28T17:00:00Z', updatedAt: '2024-11-01T10:00:00Z',  },  {    id: 'te-7', employeeId: 'emp-2', clientId: 'cl-2', projectId: 'pr-2',    date: '2024-10-29', hours: 7, rate: 6000, description: 'Фронтенд компоненте – наруџбине',    billingStatus: 'invoiced', invoiceId: 'inv-1', createdAt: '2024-10-29T18:00:00Z', updatedAt: '2024-11-02T09:00:00Z',  },  {    id: 'te-8', employeeId: 'emp-3', clientId: 'cl-3', projectId: 'pr-3',    date: '2024-10-25', hours: 3, rate: 4500, description: 'Финални pregled портала',    billingStatus: 'paid', invoiceId: 'inv-2', createdAt: '2024-10-25T17:00:00Z', updatedAt: '2024-10-28T08:00:00Z',  },  {    id: 'te-9', employeeId: 'emp-4', clientId: 'cl-3', projectId: 'pr-3',    date: '2024-10-22', hours: 6, rate: 4500, description: 'Респонсивни дизајн – footer секција',    billingStatus: 'paid', invoiceId: 'inv-2', createdAt: '2024-10-22T17:00:00Z', updatedAt: '2024-10-28T08:00:00Z',  },  {    id: 'te-10', employeeId: 'emp-1', clientId: 'cl-4', projectId: 'pr-4',    date: '2024-10-30', hours: 8, rate: 7000, description: 'Пентестинг – SQL инјекције',    billingStatus: 'invoiced', invoiceId: 'inv-3', createdAt: '2024-10-30T18:00:00Z', updatedAt: '2024-11-03T10:00:00Z',  },  {    id: 'te-11', employeeId: 'emp-2', clientId: 'cl-2', projectId: 'pr-2',    date: '2024-11-06', hours: 8, rate: 6000, description: 'Push нотификације – имплементација',    billingStatus: 'unbilled', invoiceId: null, createdAt: '2024-11-06T17:00:00Z', updatedAt: '2024-11-06T17:00:00Z',  },  {    id: 'te-12', employeeId: 'emp-3', clientId: 'cl-1', projectId: 'pr-5',    date: '2024-11-07', hours: 4, rate: 5200, description: 'Састанак са клијентом – CRM захтеви',    billingStatus: 'unbilled', invoiceId: null, createdAt: '2024-11-07T13:00:00Z', updatedAt: '2024-11-07T13:00:00Z',  },]// ---------- Mock Invoices ----------export const mockInvoices: Invoice[] = [  {    id: 'inv-1', invoiceNumber: 'Fakt-2024-001', clientId: 'cl-2', projectId: 'pr-2',    issueDate: '2024-11-01', dueDate: '2024-12-01', paymentTerms: 'net30',    items: [      { id: 'ii-1', timeEntryId: 'te-7', description: 'Фронтенд компоненте – наруџбине', hours: 7, rate: 6000, total: 42000 },    ],    subtotal: 42000, pdvRate: 20, pdvAmount: 8400, total: 50400,    status: 'sent', notes: 'Молимо уплатити на žiro račun у року од 30 дана.',    createdAt: '2024-11-01T10:00:00Z', updatedAt: '2024-11-01T10:00:00Z',  },  {    id: 'inv-2', invoiceNumber: 'Fakt-2024-002', clientId: 'cl-3', projectId: 'pr-3',    issueDate: '2024-10-28', dueDate: '2024-11-27', paymentTerms: 'net30',    items: [      { id: 'ii-2', timeEntryId: 'te-8', description: 'Финални pregled портала', hours: 3, rate: 4500, total: 13500 },      { id: 'ii-3', timeEntryId: 'te-9', description: 'Респонсивни дизајн – footer', hours: 6, rate: 4500, total: 27000 },    ],    subtotal: 40500, pdvRate: 20, pdvAmount: 8100, total: 48600,    status: 'paid', notes: '',    createdAt: '2024-10-28T08:00:00Z', updatedAt: '2024-10-28T08:00:00Z',  },  {    id: 'inv-3', invoiceNumber: 'Fakt-2024-003', clientId: 'cl-4', projectId: 'pr-4',    issueDate: '2024-11-03', dueDate: '2024-11-18', paymentTerms: 'net15',    items: [      { id: 'ii-4', timeEntryId: 'te-10', description: 'Пентестинг – SQL инјекције', hours: 8, rate: 7000, total: 56000 },    ],    subtotal: 56000, pdvRate: 20, pdvAmount: 11200, total: 67200,    status: 'overdue', notes: '',    createdAt: '2024-11-03T10:00:00Z', updatedAt: '2024-11-03T10:00:00Z',  },]// ---------- Formatting Helpers ----------/** Format number as RSD currency: "55.000,00 RSD" */export function formatRSD(amount: number): string {  const formatted = new Intl.NumberFormat('sr-RS', {    minimumFractionDigits: 2,    maximumFractionDigits: 2,  }).format(amount)  return `${formatted} RSD`}/** Format hours: "8,00" or "4,50" */export function formatHours(hours: number): string {  return hours.toFixed(2).replace('.', ',')}/** Format date to Serbian locale: "04.11.2024" */export function formatDate(dateStr: string): string {  const d = new Date(dateStr)  return new Intl.DateTimeFormat('sr-RS', {    day: '2-digit',    month: '2-digit',    year: 'numeric',  }).format(d)}/** Get Serbian month name for a given month string like "2024-11" */export function getMonthLabel(monthStr: string): string {  const months = [    'Јануар', 'Фебруар', 'Март', 'Април', 'Мај', 'Јун',    'Јул', 'Август', 'Септембар', 'Октобар', 'Новембар', 'Децембар',  ]  const [year, month] = monthStr.split('-')  return `${months[parseInt(month, 10) - 1]} ${year}`}/** Calculate PDV amount from subtotal */export function calculatePDV(subtotal: number, pdvRate: number): number {  return Math.round((subtotal * pdvRate) / 100)}/** Round hours based on settings */export function roundHours(hours: number, roundTo: 'none' | '0.5' | '1'): number {  if (roundTo === 'none') return hours  if (roundTo === '0.5') return Math.round(hours * 2) / 2  return Math.round(hours)}/** Generate invoice number from prefix and counter */export function generateInvoiceNumber(prefix: string, counter: number): string {  return `${prefix}-${new Date().getFullYear()}-${String(counter).padStart(3, '0')}`}// ---------- Report Calculation Helpers ----------export function calculateRevenueByClient(  entries: TimeEntry[],  clients: Client[],  pdvRate: number,): RevenueByClient[] {  const map = new Map<string, RevenueByClient>()  const billableEntries = entries.filter((e) =>    e.billingStatus !== 'unbilled'  )  for (const entry of billableEntries) {    const client = clients.find((c) => c.id === entry.clientId)    if (!client) continue    const existing = map.get(entry.clientId)    const revenue = entry.hours * entry.rate    if (existing) {      existing.totalHours += entry.hours      existing.totalRevenue += revenue      existing.totalPDV += calculatePDV(revenue, pdvRate)      if (entry.invoiceId) existing.invoiceCount += 1    } else {      map.set(entry.clientId, {        clientId: entry.clientId,        clientName: client.name,        totalHours: entry.hours,        totalRevenue: revenue,        totalPDV: calculatePDV(revenue, pdvRate),        invoiceCount: entry.invoiceId ? 1 : 0,      })    }  }  return Array.from(map.values()).sort((a, b) => b.totalRevenue - a.totalRevenue)}export function calculateRevenueByProject(  entries: TimeEntry[],  projects: Project[],  clients: Client[],  pdvRate: number,): RevenueByProject[] {  const map = new Map<string, RevenueByProject>()  const billableEntries = entries.filter((e) =>    e.billingStatus !== 'unbilled'  )  for (const entry of billableEntries) {    const project = projects.find((p) => p.id === entry.projectId)    const client = clients.find((c) => c.id === entry.clientId)    if (!project || !client) continue    const existing = map.get(entry.projectId)    const revenue = entry.hours * entry.rate    if (existing) {      existing.totalHours += entry.hours      existing.totalRevenue += revenue      existing.totalPDV += calculatePDV(revenue, pdvRate)    } else {      map.set(entry.projectId, {        projectId: entry.projectId,        projectName: project.name,        clientName: client.name,        totalHours: entry.hours,        totalRevenue: revenue,        totalPDV: calculatePDV(revenue, pdvRate),      })    }  }  return Array.from(map.values()).sort((a, b) => b.totalRevenue - a.totalRevenue)}export function calculateRevenueByEmployee(  entries: TimeEntry[],  employees: Employee[],): RevenueByEmployee[] {  const map = new Map<string, RevenueByEmployee>()  for (const entry of entries) {    const employee = employees.find((e) => e.id === entry.employeeId)    if (!employee) continue    const existing = map.get(entry.employeeId)    const revenue = entry.hours * entry.rate    if (existing) {      existing.totalHours += entry.hours      existing.totalRevenue += revenue      if (entry.billingStatus !== 'unbilled') {        existing.billableHours += entry.hours      } else {        existing.nonBillableHours += entry.hours      }    } else {      map.set(entry.employeeId, {        employeeId: entry.employeeId,        employeeName: employee.name,        totalHours: entry.hours,        totalRevenue: revenue,        billableHours: entry.billingStatus !== 'unbilled' ? entry.hours : 0,        nonBillableHours: entry.billingStatus === 'unbilled' ? entry.hours : 0,      })    }  }  return Array.from(map.values()).sort((a, b) => b.totalRevenue - a.totalRevenue)}export function calculateMonthlySummary(  entries: TimeEntry[],  invoices: Invoice[],): MonthlySummary[] {  const map = new Map<string, MonthlySummary>()  for (const entry of entries) {    const monthKey = entry.date.substring(0, 7)    const existing = map.get(monthKey)    const revenue = entry.hours * entry.rate    if (existing) {      existing.totalHours += entry.hours      existing.totalRevenue += revenue    } else {      map.set(monthKey, {        month: monthKey,        label: getMonthLabel(monthKey),        totalHours: entry.hours,        totalRevenue: revenue,        invoiceCount: 0,        paidAmount: 0,        outstandingAmount: 0,      })    }  }  for (const inv of invoices) {    const monthKey = inv.issueDate.substring(0, 7)    const existing = map.get(monthKey)    if (existing) {      existing.invoiceCount += 1      if (inv.status === 'paid') {        existing.paidAmount += inv.total      } else if (inv.status !== 'cancelled') {        existing.outstandingAmount += inv.total      }    }  }  return Array.from(map.values()).sort((a, b) => b.month.localeCompare(a.month))}// ---------- Invoice Generation ----------export function buildInvoiceItems(  timeEntries: TimeEntry[],  projects: Project[],): InvoiceItem[] {  return timeEntries.map((entry, idx) => {    const project = projects.find((p) => p.id === entry.projectId)    return {      id: `ii-${Date.now()}-${idx}`,      timeEntryId: entry.id,      description: entry.description || `${project?.name || 'Пројекат'} – ${formatDate(entry.date)}`,      hours: entry.hours,      rate: entry.rate,      total: entry.hours * entry.rate,    }  })}export function calculateInvoiceTotals(items: InvoiceItem[], pdvRate: number) {  const subtotal = items.reduce((sum, item) => sum + item.total, 0)  const pdvAmount = calculatePDV(subtotal, pdvRate)  return { subtotal, pdvAmount, total: subtotal + pdvAmount }}// ---------- Due date helper ----------export function calculateDueDate(issueDate: string, terms: PaymentTerms): string {  const d = new Date(issueDate)  const daysMap: Record<PaymentTerms, number> = {    net15: 15, net30: 30, net45: 45, net60: 60, on_receipt: 0,  }  d.setDate(d.getDate() + daysMap[terms])  return d.toISOString().split('T')[0]}// ---------- API helpers (thin wrappers) ----------const API_BASE = '/api/time-billing'